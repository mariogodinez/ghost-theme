{{!< default}}
{{! The tag above means - insert everything in this file into the {body} of the default.hbs template }}

{{! The big featured header }}
<header class="main-header {{#if @blog.cover}}" style="background-size:cover; background-image: url({{@blog.cover}}){{else}}no-cover{{/if}}">
    <div class="opaco">
        <nav class="main-nav overlay clearfix">
            {{#if @blog.logo}}
                <a class="blog-logo logo-back" href="{{@blog.url}}">
                    <figure class="logo hvr-buzz-out">
                        <img src="{{@blog.logo}}" alt="{{@blog.title}}" />
                    </figure>
                </a>
            {{/if}}
        </nav>

        <div class="vertical">
            <div class="main-header-content inner">
                <h1 class="page-title">{{@blog.title}}</h1>
                <h2 class="page-description">
                    {{@blog.description}}
                </h2>
            </div>
        </div>
    </div>  
</header>



{{! The main content area on the homepage }}
<main id="content" class="contentedor" role="main">
    {{! The tag below includes the post loop - partials/loop.hbs}}
    <article class="welcome" id="welcome">
        <div class="welcome-content">
            <h2 class="welcome-content-title text-uppercase">Soy Mario Godinez</h2>
            <h5 class="welcome-content-description">Desarrollador web Frontend. Amante de la tecnología. Y Javascript, mi pasión</h5>
            <img src="sds.png" alt="">
            <p class="welcome-content-text">
               Soy <strong>Desarrollador Web Frontend</strong>. Las tecnologías que uso para el trabajo de mi día a día son <strong>Gulp.js, React.js, HTML5/CSS3, Vue.js, y Node.js.</strong> Programar en Javascript es apasionante, te lleva a otra dimension.
            </p>
            <p class="welcome-content-text">
                Creo en la capacitacion constante, por eso incito a las personas a ser mejores profesionales.
            </p>
        </div>
        
    </article>
    {{> "loop"}}

</main>
